# Only what api/*.py actually imports at serving time — no torch (numpy does the
# matmuls in api/pipeline.py), no pyarrow/duckdb/scipy (unused here; those are for
# the scripts/ pipeline, run locally with the full set from README's "Requirements").
numpy==2.5.1
polars==1.43.2
fastapi==0.141.1
uvicorn[standard]==0.52.1
